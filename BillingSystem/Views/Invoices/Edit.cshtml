@model BillingSystem.Models.Invoice

@{
    ViewBag.Title = "Edit Invoice";
}

<div class="page-header-section">
    <div>
        <a class="btn-back" href="@Url.Action("Index", "Invoices", new { id = Model.InvoiceId })">
            <i class="fa fa-arrow-left"></i> BACK
        </a>
    </div>
    <div class="page-header-titles">
        <div class="page-label">Invoices</div>
        <h1 class="page-title"><span>Edit</span> invoice settings</h1>
    </div>
    <div style="min-width:120px;"></div>
</div>

@using (Html.BeginForm("Edit", "Invoices", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.InvoiceId)

    <div class="form-page-card">
        <div class="form-page-header">
            <i class="fa fa-circle-info"></i> Invoice Information
        </div>

        @Html.ValidationSummary(true, "", new { @class = "validation-summary" })

        <div class="form-row-grid">
            <div class="float-field">
                <label>Invoice Id</label>
                <input class="float-input" value="@Model.InvoiceId" readonly />
            </div>
            <div class="float-field">
                <label>Invoice Number</label>
                @Html.TextBoxFor(m => m.InvoiceNumber, new { @class = "float-input", @readonly = "readonly", placeholder = " " })
            </div>
            <div class="float-field">
                <label>Invoice Date</label>
                @Html.TextBoxFor(m => m.InvoiceDate, "{0:yyyy-MM-ddTHH:mm}", new { @class = "float-input", type = "datetime-local", placeholder = " " })
                @Html.ValidationMessageFor(m => m.InvoiceDate, "", new { @class = "field-error" })
            </div>
            <div class="float-field">
                <label>Customer Name</label>
                @Html.TextBoxFor(m => m.CustomerName, new { @class = "float-input", placeholder = " " })
                @Html.ValidationMessageFor(m => m.CustomerName, "", new { @class = "field-error" })
            </div>
            <div class="float-field">
                <label>Total Amount</label>
                @Html.TextBoxFor(m => m.TotalAmount, new { @class = "float-input", @readonly = "readonly", placeholder = " " })
            </div>
        </div>

        <div class="form-page-actions">
            <button type="submit" class="btn-create">
                <i class="fa fa-floppy-disk"></i> SAVE
            </button>
            <a href="@Url.Action("Details", "Invoices", new { id = Model.InvoiceId })" class="btn-back" style="margin-left:10px;">
                Cancel
            </a>
        </div>
    </div>
}
